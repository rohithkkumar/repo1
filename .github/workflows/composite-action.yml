name: composite action
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Use my composite action
        uses: rohithkkumar/repo1/.github/actions/my-composite-action
        with:
          node-version: '18'
      - name: Display result
        run: echo "Script Result ${{ steps.my-action.outputs.script-result }}"